{
  "name": "twinroute",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run build -w @inspector/contract && npm run build -w @inspector/core && npm run build -w @inspector/ui && npm run build -w @inspector/express && npm run build -w @inspector/hono",
    "dev:express": "npm run dev -w @inspector/express",
    "dev:hono": "npm run dev -w @inspector/hono",
    "start:express": "npm run start -w @inspector/express",
    "start:hono": "npm run start -w @inspector/hono",
    "test": "npm run build && vitest run",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
